# these are the server global settings
# stuff related to vhosts should go in one
# file per vhost in ../conf.d/

{
  "global": {
	"uid":1000,
	"gid":1000,
   "interface": "lo",
   "count-threads": "1",
   "server-string": "lwsws",
   "ws-pingpong-secs": "200",
   "init-ssl": "no",
   "plugin-dir":"/home/dafrito/src/parsegraph/server/lwsws/plugins"
 }
}

